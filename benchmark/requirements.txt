# Benchmark-only dependencies (not needed by the langchain_ascend library).
deepagents>=0.2
langchain-openai>=1.0
langchain>=1.3.18
# The oj-* agents additionally require the proprietary openjiuwen package
# (internal index, not published on PyPI):
#   pip install openjiuwen
# Without it those two agents are skipped and reported as build failures.
